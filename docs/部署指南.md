# PanHub 部署指南

本文档提供 PanHub 在不同平台的完整部署方案，包括本地开发、Docker、Vercel 和 Cloudflare Workers。

---

## 📋 部署前准备

### 系统要求

| 环境 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 1 核 | 2 核 |
| 内存 | 512MB | 1GB |
| 存储 | 500MB | 2GB |
| Node.js | 18.x | 20.x |

### 必需依赖

```bash
# Node.js (推荐使用 nvm)
nvm install 20
nvm use 20

# 包管理器 (任选其一)
npm install -g pnpm
# 或
npm install -g yarn

# Git
git --version
```

---

## 🚀 快速开始

### 1. 获取源码

```bash
# 克隆仓库
git clone https://github.com/wu529778790/panhub.shenzjd.com.git
cd panhub.shenzjd.com

# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 访问
# http://localhost:3000
```

### 2. 验证安装

```bash
# 运行测试
pnpm test

# 查看版本
pnpm --version
node --version
```

---

## 💻 本地开发

### 开发命令

```bash
# 启动开发服务器 (热更新)
pnpm dev

# 构建生产版本
pnpm build

# 预览生产构建
pnpm preview

# 类型检查
pnpm typecheck

# 代码格式化
pnpm format

# Lint 检查
pnpm lint
```

### 热搜存储模式

PanHub 的热搜服务支持两种存储模式：

**本地开发（推荐）**：
```bash
# 安装 better-sqlite3 以使用 SQLite 数据库（数据持久化）
pnpm install
# better-sqlite3 会自动安装，使用 SQLite 存储热搜数据
# 数据保存在 hotsearch.db 文件中
```

**优势**：
- ✅ 数据持久化，重启服务不丢失
- ✅ 支持复杂查询和统计
- ✅ 适合本地开发和调试

### 环境配置

创建 `.env` 文件（可选）：

```bash
# .env
# 日志级别: debug | info | warn | error
LOG_LEVEL=info

# 服务端口
PORT=3000

# 速率限制
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60

# 缓存配置
CACHE_TTL_MINUTES=30
CACHE_MAX_SIZE=1000

# 搜索配置
DEFAULT_CONCURRENCY=10
PLUGIN_TIMEOUT_MS=15000
```

### 目录结构

```
panhub.shenzjd.com/
├── .nuxt/                 # Nuxt 构建产物
├── .output/               # Nitro 构建产物
├── components/            # Vue 组件
│   └── Search/
├── composables/           # 组合式函数
│   └── search/            # 搜索相关
├── docs/                  # 文档
├── pages/                 # 页面
│   └── index/
│       └── index.vue      # 主页面
├── server/                # 服务端代码
│   ├── api/               # API 路由
│   ├── core/              # 核心逻辑
│   │   ├── cache/         # 缓存系统
│   │   ├── errors/        # 错误处理
│   │   ├── plugins/       # 插件系统
│   │   ├── services/      # 业务服务
│   │   ├── types/         # 类型定义
│   │   └── utils/         # 工具函数
│   └── middleware/        # 中间件
├── test/                  # 测试
│   ├── unit/              # 单元测试
│   └── integration/       # 集成测试
├── nuxt.config.ts         # Nuxt 配置
├── package.json           # 项目配置
└── vitest.config.ts       # 测试配置
```

---

## 🐳 Docker 部署

### 方式一：使用预构建镜像

```bash
# 快速启动 (内存模式)
docker run -d --name panhub \
  -p 3000:3000 \
  ghcr.io/wu529778790/panhub.shenzjd.com:latest

# 查看日志
docker logs -f panhub

# 停止容器
docker stop panhub
docker rm panhub
```

### 方式二：数据持久化 (SQLite)

```bash
# 创建数据目录
mkdir -p /opt/panhub/data

# 启动容器并挂载数据
docker run -d --name panhub \
  -p 3000:3000 \
  -v /opt/panhub/data:/app/data \
  -e LOG_LEVEL=info \
  ghcr.io/wu529778790/panhub.shenzjd.com:latest

# 验证数据持久化
curl http://localhost:3000/api/hot-search-stats
# 应返回: "mode": "sqlite"
```

### 方式三：从源码构建

```bash
# 克隆仓库
git clone https://github.com/wu529778790/panhub.shenzjd.com.git
cd panhub.shenzjd.com

# 构建镜像
docker build -t panhub:latest .

# 运行容器
docker run -d --name panhub \
  -p 3000:3000 \
  -v /opt/panhub/data:/app/data \
  panhub:latest
```

### Docker Compose

创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  panhub:
    image: ghcr.io/wu529778790/panhub.shenzjd.com:latest
    container_name: panhub
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - LOG_LEVEL=info
      - PORT=3000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

启动：
```bash
docker-compose up -d
docker-compose logs -f
```

### Docker 部署最佳实践

1. **资源限制**
```bash
docker run -d --name panhub \
  --memory=1g \
  --cpus=1 \
  -p 3000:3000 \
  -v /opt/panhub/data:/app/data \
  ghcr.io/wu529778790/panhub.shenzjd.com:latest
```

2. **日志管理**
```bash
# 查看日志
docker logs panhub

# 日志轮转
docker run -d --name panhub \
  --log-opt max-size=10m \
  --log-opt max-file=3 \
  ...
```

3. **自动重启**
```bash
docker run -d --restart unless-stopped ...
```

---

## ☁️ Vercel 部署

### 方式一：一键部署

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2Fwu529778790%2Fpanhub.shenzjd.com&project-name=panhub&repository-name=panhub.shenzjd.com)

### 方式二：手动部署

1. **导入项目**
   - 登录 Vercel 控制台
   - "Add New" → "Project"
   - 导入 GitHub 仓库

2. **配置构建**
   ```
   Framework Preset: Other
   Build Command: pnpm build
   Output Directory: .output/public
   Install Command: pnpm install --frozen-lockfile
   ```

3. **环境变量**
   ```
   LOG_LEVEL=info
   ```

4. **部署**
   - 点击 "Deploy"
   - 等待构建完成

### Vercel 注意事项

**存储模式**：
- ✅ **自动降级**：better-sqlite3 不在 dependencies 中，Vercel 不会尝试安装
- ✅ **内存模式**：自动使用内存存储，无需数据库文件
- ✅ **无依赖问题**：避免 native 模块编译失败
- ⚠️ **数据不持久**：Serverless 重启后数据丢失（正常行为）

**限制**：
- Serverless 函数超时：10 秒（免费版）
- 内存限制：1024 MB
- 无持久化存储（使用内存模式）

**配置优化**：
```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  nitro: {
    preset: 'vercel',
    // 减少打包大小
    externals: {
      inline: []
    }
  }
})
```

---

## 🌪️ Cloudflare Workers 部署

### 方式一：一键部署

[![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/wu529778790/panhub.shenzjd.com)

### 方式二：使用 Wrangler

1. **安装 Wrangler**
```bash
npm install -g wrangler
wrangler login
```

2. **配置项目**
```bash
# 创建 wrangler.toml
cat > wrangler.toml << EOF
name = "panhub"
main = ".output/server/index.mjs"
compatibility_date = "2024-01-01"

[placement]
mode = "smart"

[limits]
cpu_ms = 50000
memory = "128MiB"

[env.production]
name = "panhub-production"
EOF
```

3. **构建并部署**
```bash
# 安装依赖
pnpm install

# 构建
pnpm build

# 部署
pnpm deploy:cf
# 或
wrangler deploy
```

### Cloudflare 配置

**环境变量** (在 Workers 控制台设置)：
```
LOG_LEVEL=info
```

**KV 命名空间** (用于持久化)：
```bash
# 创建 KV 命名空间
wrangler kv:namespace create HOT_SEARCHES

# 在 wrangler.toml 中绑定
[[kv_namespaces]]
binding = "HOT_SEARCHES"
id = "your-namespace-id"
```

**修改代码支持 KV**：
```typescript
// server/core/services/hotSearchSQLite.ts
// 需要适配 Cloudflare KV 而非 SQLite
```

### Cloudflare 限制

| 项目 | 免费版 | 付费版 |
|------|--------|--------|
| 请求次数 | 10万/天 | 无限制 |
| CPU 时间 | 10ms/请求 | 50ms/请求 |
| 内存 | 128MB | 128MB |
| 持久化 | KV (1GB) | KV (更多) |
| 超时 | 10秒 | 30秒 |

---

## 🔧 生产环境优化

### 1. 性能调优

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  // 关闭开发工具
  devtools: { enabled: false },

  // 代码分割
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'nuxt'],
          ui: ['@nuxt/ui']
        }
      }
    }
  },

  // 缓存策略
  nitro: {
    routeRules: {
      '/api/search': { cache: { maxAge: 60 * 5 } },
      '/api/hot-searches': { cache: { maxAge: 60 * 1 } }
    }
  }
})
```

### 2. 安全配置

```typescript
// server/middleware/security.ts
export default defineEventHandler((event) => {
  // 安全头
  setResponseHeader(event, 'X-Content-Type-Options', 'nosniff')
  setResponseHeader(event, 'X-Frame-Options', 'DENY')
  setResponseHeader(event, 'X-XSS-Protection', '1; mode=block')

  // CORS (如果需要)
  setResponseHeader(event, 'Access-Control-Allow-Origin', 'https://your-frontend.com')
})
```

### 3. 监控配置

```bash
# 环境变量
LOG_LEVEL=info
# 或
LOG_LEVEL=debug  # 开发环境
```

日志输出示例：
```
[INFO] [search-service] 搜索开始: kw=测试, src=plugin
[INFO] [plugin-manager] 执行插件: hunhepan (优先级: 1)
[WARN] [fetch] 请求超时: hunhepan (3000ms)
[ERROR] [error-handler] 插件执行失败
[INFO] [search-service] 搜索完成: 12 条结果
```

### 4. 数据备份

**SQLite 模式**：
```bash
# 定时备份
0 2 * * * cp /opt/panhub/data/hot-searches.db /backup/panhub-$(date +\%Y\%m\%d).db

# 或使用卷快照
docker run --volumes-from panhub -v /backup:/backup alpine tar czf /backup/panhub-data.tar.gz /app/data
```

---

## 🔍 故障排查

### 1. 服务无法启动

**症状**：`pnpm dev` 或 `docker logs` 显示错误

**排查步骤**：
```bash
# 1. 检查端口占用
lsof -i :3000
netstat -tulpn | grep 3000

# 2. 检查 Node 版本
node --version  # 应 >= 18

# 3. 清理缓存
rm -rf node_modules .nuxt .output
pnpm install

# 4. 查看详细日志
LOG_LEVEL=debug pnpm dev
```

### 2. 搜索无结果

**可能原因**：
- 插件配置错误
- 网络问题
- 目标网站变更

**排查**：
```bash
# 检查健康状态
curl http://localhost:3000/api/health

# 查看插件列表
# 应返回所有已注册插件

# 测试单个插件
curl "http://localhost:3000/api/search?kw=test&src=plugin&plugins=hunhepan"
```

### 3. Docker 数据丢失

**问题**：容器重启后热搜数据消失

**解决**：
```bash
# 检查是否挂载卷
docker inspect panhub | grep Mounts

# 正确的挂载方式
docker run -d \
  -v /host/path:/app/data \
  ...
```

### 4. Vercel 超时

**问题**：搜索请求在 10 秒后超时

**解决**：
- 减少并发数
- 使用缓存
- 考虑升级到 Pro 版（50秒超时）
- 或迁移到 Docker/VPS

### 5. Cloudflare Workers 内存不足

**问题**：`Exceeded memory limit`

**解决**：
- 减少并发数
- 优化缓存策略
- 考虑使用 Durable Objects
- 或迁移到 VPS

---

## 📊 性能基准

### 本地开发 (MacBook M1)

| 操作 | 耗时 | 内存占用 |
|------|------|---------|
| 冷启动 | 1.2s | 150MB |
| 热搜索 | 50ms | 180MB |
| 搜索 (5插件) | 2-5s | 250MB |
| 并发 10 请求 | 3-6s | 300MB |

### Docker (2核2GB)

| 操作 | 耗时 | CPU | 内存 |
|------|------|-----|------|
| 启动 | 3s | 50% | 200MB |
| 搜索 | 2-4s | 80% | 250MB |

### Vercel (免费版)

| 操作 | 耗时 | 说明 |
|------|------|------|
| 冷启动 | 1-2s | Serverless 函数 |
| 搜索 | 3-8s | 受 10s 超时限制 |
| 并发 | 10 req | 免费版限制 |

---

## 🔄 更新升级

### 1. 更新代码

```bash
# 拉取最新代码
git pull origin main

# 更新依赖
pnpm install

# 重新构建
pnpm build

# 重启服务
# Docker
docker-compose restart

# PM2
pm2 restart panhub

# 直接运行
pnpm dev
```

### 2. 数据迁移

**版本升级时**：
```bash
# 备份数据
cp data/hot-searches.db hot-searches.db.backup

# 更新代码后，检查是否需要迁移
# 通常不需要，SQLite 会自动兼容

# 如果出错，恢复备份
cp hot-searches.db.backup data/hot-searches.db
```

### 3. 回滚

```bash
# 回滚到上一版本
git log --oneline  # 查看历史
git checkout <commit-hash>

# 重新部署
pnpm build && pnpm restart
```

---

## 📝 部署清单

部署前检查清单：

- [ ] Node.js 版本 >= 18
- [ ] 依赖已安装 (`pnpm install`)
- [ ] 测试通过 (`pnpm test`)
- [ ] 环境变量配置
- [ ] 端口未被占用
- [ ] 数据目录权限正确 (Docker)
- [ ] 防火墙开放端口
- [ ] 域名解析配置 (生产)
- [ ] SSL 证书配置 (生产)
- [ ] 日志系统配置
- [ ] 监控告警设置
- [ ] 备份策略制定

---

## 🆘 技术支持

### 问题反馈

1. **GitHub Issues**: https://github.com/wu529778790/panhub.shenzjd.com/issues
2. **Discussions**: GitHub Discussions
3. **Email**: wu529778790@gmail.com

### 社区资源

- **文档**: 查看 `docs/` 目录
- **示例**: 查看 `test/` 目录
- **更新日志**: 查看 `CHANGELOG.md`

---

**文档版本**: v1.0
**最后更新**: 2026-01-07
**维护者**: PanHub Team
